# Example environment variables (DO NOT COMMIT real secrets)
NODE_ENV=production
# Set this to your Fly app URL after deployment (e.g. https://quizard-backend.fly.dev)
BACKEND_URL=
# Comma-separated allowed origins for socket.io (front-end URLs)
ALLOWED_ORIGINS=https://quizard-weld.vercel.app,https://quizard-ks39tuqre-kobiljonkosimovs-projects.vercel.app
# Optional admin secret if you add auth later
HOST_SECRET=
